-- baudrates
--   - test maybe up to 57600...
  + minibee without xbee yes, but with XBee no...

-- PING
  - could have multiple ping pins, if more ultrasound sensors
  - use an interrupt and timer to measure ping time, instead of waiting.
  
-- maybe integrate
  - CapSense
  - stepper motor
  - LED matrix
  - more generalised interface to TWI/I2C... reading different addresses, different setups...


-- testing
  + analogIn
  + digitalIn
  + SHT
  + TWI
  + Ping
  + digitalOut
  + PWM
  + message 'R'
  + message 'L'
  - message 'M'
  + custom message
  + custom pins
	+ custom pins... offset

-- update times
	+ 25 ms
	+ 50 ms
	+ 100 ms
	- 200 ms
	- 1000 ms
-- samplesPerMsg
	+ 1
	- 2
	- 5
	- 10

-- add in the version and revision info
-- add a motor message ('M') and optional stepper motor control


--- SuperCollider side
  + gui bug for msg interval
  + removeNode when changing config
  + redundancy in sending id and config
  + add in support for custom message sending
  + add in support for custom data receiving
  + gui interface slightly wider to read names of configs
  + status indicator for inactive
  + update of gui when data starts flowing
  + scrollview for the nodes